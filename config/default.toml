target_density = 40
critical_density = 10
min_gap_area = 5
# minimum detection confidence counted toward density; raising it undercounts real plants, lowering it counts more false positives
conf_threshold = 0.2
crs = "EPSG:25832"
detection_subfolder = "crop_canola"
